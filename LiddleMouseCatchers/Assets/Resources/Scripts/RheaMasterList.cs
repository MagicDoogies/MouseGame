using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RheaMasterList : MonoBehaviour
{
    public List<Critters> insectList;
    
    // Start is called before the first frame update
    void Start()
    {
        insectList = new List<Critters>();
    }
    // Update is called once per frame
    void Update()
    {
       
    }
   

    
}
